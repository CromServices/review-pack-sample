{
  "name": "crom-review-pack-sample",
  "version": "1.0.0",
  "description": "Public sample of a Crom Services code-review delivery pack",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
